# Player/PlayerGroup land here in a follow-up commit — this stub exists
# so ANTHIAS_SERVICE=fleet can boot and migrate cleanly (an empty app
# is still a valid Django app) before any fleet-specific schema is
# layered on, isolating plumbing failures from model-design ones.
